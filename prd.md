# EatEat 产品文档

| 项 | 内容 |
| --- | --- |
| 版本 | 1.0 最终稿 |
| 日期 | 2026-09-23 |
| 状态 | 已确认，作为设计与开发依据 |
| 形态 | 微信内 H5 |
| 角色名 | 需求原文中的 coker 统一为 Cooker（做饭端）；另一端为 Eater（点餐端） |

EatEat 是只给一对人用的私人厨房。一个人把会做的菜放上菜单，另一个人点早上、中午或晚上。做完的那一餐，两个人都能翻看。

一个厨房里只有一个做饭的人和一个点餐的人。没有支付、配送、公开菜谱和陌生人。

## 1. 已确认规则

以下规则在本版生效。

1. 一个厨房固定一个 Cooker、一个 Eater。本版绑定后角色不变，不做对调。
2. 点餐后进入待接单。Cooker 接单或拒绝；拒绝必须留一句话。
3. 一笔订单是某一天的某一个餐次，包含 1 到 6 道菜。同一天、同一餐次同时只能有一笔进行中的订单。
4. 只能点今天、明天、后天。餐次只有早上、中午、晚上。不设点餐截止时间。
5. 没有封面不能上架。封面来自做饭的人确认使用的 AI 图，或自己上传的图。
6. 小红书或抖音链接只生成草稿，不直接上架，也不把平台原图放进菜单。
7. 解除绑定立即生效，双方都不再看到旧菜、旧订单和旧记录，也不提供导出。
8. 本版只在应用内显示角标，不发微信订阅消息。
9. 必须在微信内使用。一个微信身份只能加入一个厨房。

## 2. 体验原则

| 原则 | 落到界面上 |
| --- | --- |
| 只服务这一对人 | 没有推荐流、排行、陌生人或其他厨房的菜 |
| 点餐是商量 | 做饭的人可以拒绝，拒绝时留一句话 |
| 菜单只放真会做的 | 草稿和下架菜，点餐端看不见 |
| 记录是共同记忆 | 同一条时间线。点餐端不能改、不能删 |
| 语气像冰箱纸条 | 用「早上 / 这一餐 / 记下」。界面不出现「用户 / 履约 / SKU」 |

## 3. 名词

| 名词 | 定义 |
| --- | --- |
| 厨房 | 一对绑定关系的数据边界。菜、订单、记录都属于厨房 |
| 餐次 | 界面写作早上、中午、晚上，系统内对应早餐、午餐、晚餐 |
| 菜品 | 一道可被管理的菜。只有已上架的才出现在菜单 |
| 订单 | 某一天、某一个餐次下的一次点餐，里面有一道或多道菜 |
| 做饭记录 | 某一次实际做过的饭，含照片或文字。可关联订单，也可以单独记 |
| 快照 | 订单提交或记录发布时复制的菜名和封面。之后改菜、下架、删除，都不改已经复制的内容 |

## 4. 信息架构

未绑定成功前，不出现底部业务入口，只走身份选择和邀请。

设置不占底部导航，放在各端首页右上角。设置里有称呼、绑定对象、解除绑定。本版没有角色对调入口。

| 端 | 底部入口 | 里面有什么 | 打开后的落点 |
| --- | --- | --- | --- |
| Cooker | 待做 | 按用餐时间排列的订单。接单、拒绝、完成 | 是 |
| Cooker | 菜品 | 草稿、已上架、已下架。链接导入与自己写 | 否 |
| Cooker | 记录 | 时间线，以及记下这餐 | 否 |
| Eater | 菜单 | 只含已上架菜。选定这一餐后加入菜品 | 是 |
| Eater | 订单 | 自己点过的餐和当前状态 | 否 |
| Eater | 记录 | 与做饭端相同的时间线，只读 | 否 |

## 5. 主路径

1. 先打开的人选择「我来做饭」或「我来点餐」，生成一间待加入的厨房。
2. 另一个人打开邀请链接或输入邀请码，自动成为互补角色，绑定完成。
3. Cooker 用链接或手写做出草稿，确认封面后上架。这道菜出现在 Eater 的菜单里。
4. Eater 选择日期和早上 / 中午 / 晚上，提交点餐。订单出现在 Cooker 的待做里。
5. Cooker 接单，做完后标记完成。此时可以记下这餐，也可以跳过。
6. 双方在记录里打开这餐，看到同一组照片和文字。

## 6. 绑定

本地没有有效厨房时，只出现身份选择。不能浏览菜、不能下单、不能看记录。已经绑定的人再次打开，按自己的角色进入首页，不再询问身份。

### 6.1 流程

| 顺序 | 页面 | 规则 |
| --- | --- | --- |
| 1 | 身份选择 | 两个选项：我来做饭、我来点餐。选定后才创建厨房 |
| 2 | 邀请 | 展示 6 位邀请码和一条链接。有效期 48 小时。刷新后旧码立即失效 |
| 3 | 等待 | 创建者留在等待页。此阶段没有菜单，对方还看不到任何菜 |
| 4 | 加入确认 | 被邀请人看到对方的称呼和角色。自己的角色自动取互补，不能改选 |
| 5 | 完成 | 双方进入各自首页 |

### 6.2 身份选择文案

我来做饭。副文：我来上架菜、接下这一餐、记下做过的饭。按钮：生成邀请，等对方来点餐。

我来点餐。副文：我来点早上、中午和晚上。按钮：生成邀请，等对方来做饭。

### 6.3 账号与称呼

必须在微信内打开。不在微信内时，页面提示用微信打开，不创建厨房。

微信身份用来识别是不是同一个人。一个微信身份同时只能属于一个厨房。换一个微信身份才能再开一间。本版没有家庭号、多人共厨和游客模式。

每人有一个 2 到 8 字的厨房称呼。邀请页和记录使用这个称呼。第一次默认是「做饭的人」或「点餐的人」，设置里可以改。允许把微信昵称填进称呼，保存前可以改掉。

### 6.4 邀请冲突

| 情况 | 处理 |
| --- | --- |
| 码过期或写错 | 停留在输入页，说明要对方重新发。不创建空厨房 |
| 自己打开自己的邀请 | 回到等待页，不重复加入 |
| 已经绑定，又打开别人的邀请 | 说明你已经有厨房。不切换、不覆盖 |
| 自己有一间还没人加入的厨房，又去接受邀请 | 先说明：接受后，这间空厨房会被放弃。确认后才加入 |
| 对方已经解除绑定 | 下次打开回到身份选择。旧厨房不再进入 |

### 6.5 解除绑定

任一方都可以发起解除，立即生效，不需要对方同意。解除后两个人都回到身份选择。

菜品、订单、记录保留在服务端，任何一端都不再展示，也不提供导出。重新绑定得到一间新厨房，旧内容不跟过来。

## 7. Cooker 做饭端

做饭端管理菜品，用链接或手写录入，生成或上传封面，决定上架，处理点餐，并上传做饭记录。

### 7.1 菜品字段

菜品列表分成草稿、已上架、已下架。点餐端只能看到已上架。做饭端可以预览上架后的样子，预览不等于上架。

| 字段 | 约束 | 谁看见 |
| --- | --- | --- |
| 菜名 | 必填，最多 20 字 | 双方，出现在菜单和订单快照 |
| 简介 | 选填，最多 80 字 | 双方 |
| 食材 | 选填，一条一行 | 双方，在菜品详情 |
| 步骤 | 选填，最多 30 步，每步最多 200 字 | 双方，详情里默认收起 |
| 预计耗时 | 选填，单位为分钟 | 双方 |
| 份量 | 选填，例如 2 人份。一餐里同一道菜只出现一次，不另选份数 | 双方 |
| 封面 | 上架前必填。来自自己上传，或确认使用的 AI 图 | 双方 |
| 来源链接 | 链接导入时写入，只读 | 仅做饭端编辑页。菜单上不出现平台名 |

### 7.2 两条录入路径

菜品页有两个并列入口：「从链接添加」和「自己写」。两条路径得到同一种草稿。链接失败时，手写页和成功解析后的编辑页是同一张表单。

| 路径 | 输入 | 产出 |
| --- | --- | --- |
| 从链接添加 | 一条小红书或抖音链接 | 可编辑草稿：菜名、食材、步骤、耗时。不自动上架，不使用平台原图 |
| 自己写 | 菜品表单 | 同样的草稿。没有来源链接 |

链接识别：

| 识别 | 处理 |
| --- | --- |
| 小红书网页或短链 | 发起解析。域名包含 xiaohongshu.com、xhslink.com |
| 抖音网页或短链 | 发起解析。域名包含 douyin.com、v.douyin.com、iesdouyin.com |
| 其他链接 | 不发请求。提示只能用小红书或抖音，并给出「改为自己写」 |
| 解析超过 20 秒，或内容打不开 | 进入编辑页，链接留在来源里。提示：这条没解析出来，可以直接写 |
| 只有标题，没有步骤 | 仍然生成草稿，步骤留空，由做饭的人补 |
| 解析成功 | 进入编辑页，字段可改。保存前不上架 |

解析结果只作为私人草稿的起步文字。给对方看的图，来自自己上传，或来自确认后的 AI 封面。来源链接不出现在点餐端。

### 7.3 AI 封面

编辑页提供「生成封面」。生成依据只有菜名、简介和主要食材，不用整篇步骤。画面是一张方形食物照片，画面中没有文字、logo、人脸和包装品牌。

| 状态 | 界面 | 能否上架 |
| --- | --- | --- |
| 未生成 | 显示生成按钮，也可以直接上传 | 没有封面就不能上架 |
| 生成中 | 按钮变为进行中，表单其他内容仍可编辑 | 不可以 |
| 已出图，未采用 | 候选图旁边是「用这张」和「重新生成」。不自动替换当前封面 | 仍看当前已采用的封面 |
| 已采用 | 封面位显示这张图，可以再换成上传图 | 满足其他条件就可以 |
| 失败 | 说明没生成成功，可重试或改为上传。草稿仍在 | 不可以，直到有封面 |

每道菜每天最多重新生成 5 次。次数用完后仍可上传自己的图。

### 7.4 上架、下架、删除

| 动作 | 条件 | 对点餐端的影响 |
| --- | --- | --- |
| 上架 | 有菜名，有封面，并且简介和步骤至少有一项 | 菜单立即可见 |
| 保存草稿 | 至少有菜名 | 不可见 |
| 下架 | 当前是已上架 | 菜单消失。已经点到的订单保留菜名和封面快照 |
| 删除 | 没有待接单或已接单的订单引用这道菜 | 从做饭端列表消失。历史订单仍留快照 |
| 编辑已上架 | 允许改文字和封面 | 菜单显示新内容。已存在订单不改写快照 |

### 7.5 处理点餐

做饭端打开应用后落在待做。每一笔待接单都可以接单或拒绝。

| 动作 | 规则 |
| --- | --- |
| 接单 | 订单从待接单变为已接单。点餐端看得到 |
| 拒绝 | 必须写一句话，最多 40 字。订单变为已拒绝，点餐端看到这句话 |
| 完成 | 只在已接单后可以标记。完成后引导「记下这餐」，允许跳过，之后可在订单里补记 |
| 取消 | 完成前可以取消。必须留一句话，最多 40 字，对方可见 |

### 7.6 验收

- 从链接添加后，对方菜单里没有这道菜，直到上架。
- 不支持的链接不会发出解析，同一张表单可以改为自己写。
- 生成封面失败时，草稿还在，上架处说明缺封面。
- 下架不影响已经接下的那一单里的菜名和封面。
- 被待接单或已接单引用的菜不能删除。

## 8. Eater 点餐端

点餐端只浏览已上架的菜，选定这一餐后下单，查看订单状态，并和对方一起看做饭记录。

### 8.1 菜单

菜单按最近上架的顺序排列。本版没有搜索、分类和推荐。

菜品卡片展示封面、菜名、耗时和份量。详情里有简介和食材，步骤默认折叠。展开步骤是为了让点餐的人知道这道菜大概是什么。

空菜单文案：「还没有上架的菜。」没有替对方创建菜品的入口。

### 8.2 点一餐

菜单顶部固定「这一餐」：先选今天、明天或后天，再选早上、中午或晚上。来不及做，由做饭的人拒绝并留一句话。

| 规则 | 说明 |
| --- | --- |
| 日期 | 今天、明天、后天 |
| 餐次 | 早上、中午、晚上，三选一 |
| 菜品 | 1 到 6 道。同一道菜在这一餐里只加一次 |
| 备注 | 选填，最多 100 字 |
| 进行中的同一餐 | 同一天、同一餐次已有待接单或已接单时，不能再开一笔。底部按钮改为「查看已点的这一餐」 |
| 终态之后 | 已完成、已拒绝、已取消之后，可以再点同一天的同一餐 |
| 提交后 | 不能改菜。想换菜，在待接单时取消，再重新点 |

确认页重复展示日期、餐次、菜品封面、菜名和备注。主按钮是「点这餐」。成功后停在这笔订单上，并写明：对方打开 EatEat 后，会在待做里看到。

### 8.3 订单在点餐端

| 状态 | 点餐端能做的事 |
| --- | --- |
| 待接单 | 查看，或取消并留一句话 |
| 已接单 | 查看，或在完成前取消并留一句话 |
| 已完成 | 查看快照。对方已记下时，可以进入那条记录 |
| 已拒绝 | 查看那句话，然后可以重新点 |
| 已取消 | 只查看 |

点餐端不能接单，不能改菜品内容，不能上传做饭记录。

### 8.4 验收

- 草稿和已下架菜不出现在菜单。
- 未选餐次、未选菜时，不能提交。
- 同一天同一餐已有进行中订单时，提交入口变成查看原订单。
- 对方拒绝后，点餐端能看到那句话，并能重新点这一餐。
- 菜在下单后被下架，这笔订单里的封面和菜名不变。

## 9. 做饭记录

做饭记录属于厨房。做饭端负责上传、修改和删除。点餐端只能看。

列表按用餐日期倒序。同一天里，晚上在上、中午居中、早上在下。

| 字段 | 规则 |
| --- | --- |
| 日期 | 必填。从订单进入时带出订单日期。单独记时默认今天，可改 |
| 餐次 | 必填。早上、中午或晚上 |
| 菜品 | 可选多道，从这间厨房的全部菜里选，包含未上架。也可以不关联具体菜。关联时复制当时的菜名 |
| 关联订单 | 从完成订单进入时自动带上。一笔订单最多一条记录。没有订单也可以单独记 |
| 照片 | 最多 9 张。照片和文字至少有一样才能发布 |
| 文字 | 最多 300 字。没有照片时必填 |

做饭端的记录页有「记下这餐」。完成订单时也会邀请记一次，可以跳过，之后在该订单里补记。发布后可以改文字、增删照片。删除后，对方那里也一起消失。

点餐端没有上传入口。卡片展示日期、餐次、菜名、首图和一句话。空状态文案：「还没有记下的一餐。」

## 10. 权限

| 能力 | Cooker | Eater |
| --- | --- | --- |
| 选择身份并创建邀请 | 可以 | 可以 |
| 接受互补角色的邀请 | 可以 | 可以 |
| 新建、编辑、上架、下架、删除菜 | 可以 | 不可以 |
| 链接导入、生成封面 | 可以 | 不可以 |
| 浏览已上架菜 | 可以预览 | 可以点餐 |
| 提交点餐 | 不可以 | 可以 |
| 接单、拒绝、标记完成 | 可以 | 不可以 |
| 取消未完成订单 | 可以 | 可以 |
| 上传、修改、删除做饭记录 | 可以 | 不可以 |
| 查看做饭记录 | 可以 | 可以 |
| 发起解除绑定 | 可以 | 可以 |

### 角标

本版不发微信订阅消息。角标规则如下。

- 做饭端「待做」：有待接单时显示数量。
- 点餐端「订单」：状态变为已接单、已拒绝或已完成，且本人还没打开时显示数量。
- 双方「记录」：对方新发布一条记录，且自己还没打开记录页时显示数量。

点餐成功页写明对方需要打开 EatEat 才能看到。对方一直不打开时，订单会停在待接单。这是本版的已知取舍。

## 11. 状态

### 11.1 菜品

| 状态 | 如何进入 | 点餐端 |
| --- | --- | --- |
| 草稿 | 保存时未满足上架条件，或主动存成草稿 | 不可见 |
| 已上架 | 通过上架校验 | 可见，可加入这一餐 |
| 已下架 | 从已上架下架 | 不可见。旧订单保留快照 |

删除不是第四种展示状态。删除后，做饭端列表也不再出现。历史订单里的快照还在。

### 11.2 订单

| 状态 | 谁推进 | 下一步 |
| --- | --- | --- |
| 待接单 | 点餐端提交 | 接单、拒绝，或任一方取消 |
| 已接单 | 做饭端接单 | 完成，或任一方取消 |
| 已完成 | 做饭端标记完成 | 终态。可补一条做饭记录 |
| 已拒绝 | 做饭端拒绝并留言 | 终态。点餐端可重新点这一餐 |
| 已取消 | 任一方在完成前取消 | 终态 |

订单在提交时复制菜名和封面。之后改菜、下架、删除，都不改这笔订单上已经复制的名字和图。

## 12. 异常

失败时停在当前页，保留已经填写的内容。不把半成品直接上架，也不直接下单。

| 异常 | 用户看到的结果 |
| --- | --- |
| 链接平台不对 | 不请求解析，可改为自己写 |
| 解析超时或内容打不开 | 进入编辑页，来源保留，步骤可能为空 |
| 封面生成失败 | 草稿保留，上架被挡住 |
| 上架时缺封面，或既无简介也无步骤 | 上架不可用，并写明缺哪一项 |
| 删除仍被进行中订单使用的菜 | 不允许，只能下架 |
| 重复点同一笔进行中的餐 | 打开原订单，不新建 |
| 记录没有照片也没有文字 | 不能发布 |
| 上传或提交时断网 | 留在当前页，保留输入，可再试 |

## 13. 本版范围

### 13.1 要做完

| 能力 | 完成的样子 |
| --- | --- |
| 身份选择与绑定 | 未绑定进不了菜单和菜品。绑定后各进各的首页 |
| 手动录入菜品 | 能存草稿、编辑、上架、下架、删除 |
| 链接生成步骤 | 小红书或抖音链接变成可编辑草稿，失败后能在同一张表单里自己写 |
| AI 封面 | 按菜品信息出图，确认后才能当封面。失败可改为上传 |
| 点餐 | 从已上架菜里点今天到后天的早上、中午或晚上 |
| 处理订单 | 做饭端能接单、拒绝、完成。双方能取消未完成订单 |
| 做饭记录 | 做饭端上传，双方在同一时间线查看 |

### 13.2 本版不做

| 不做 | 原因 |
| --- | --- |
| 支付、配送、外卖 | 这一餐发生在家里 |
| 三人以上的厨房、公开社区、关注 | 边界就是这一对人 |
| 点餐端上传记录、评论、点赞 | 本版只要求双方查看 |
| 搜索、分类、智能推荐 | 菜的数量是一对人的尺度 |
| 份数、规格、跨多餐的购物车 | 一笔订单就是一餐 |
| 宵夜、加餐、任意时刻 | 只有早上、中午、晚上 |
| 点餐截止时间 | 来不及就拒绝 |
| 播放平台视频、使用平台原封面 | 只把步骤文字放进私人草稿 |
| 营养分析、购物清单 | 不在这次闭环里 |
| 微信订阅消息 | 本版用应用内角标 |
| 角色对调 | 放到下一版，且必须对方同意 |

### 13.3 下一版候选

绑定和点餐跑通之后再做，不进入本版页面。

| 方向 | 价值 |
| --- | --- |
| 微信订阅消息 | 对方不打开 H5 时，仍能知道有新的一餐 |
| 对方同意后对调角色 | 两个人轮流做饭。对调不删除菜、订单和记录 |
| 从订单食材收成采购清单 | 接单之后少问一次家里有没有 |
| 点餐端许愿菜单上没有的菜 | 想吃的东西先被记下来，不直接改菜单 |

## 14. 上线后看什么

本版先看这四件事是否发生，不设目标数字。

| 现象 | 说明 |
| --- | --- |
| 邀请发出后，对方完成加入 | 绑定这条路是通的 |
| 每周都有人点餐，并且多数在接单后完成 | 菜单真的在被吃 |
| 链接失败时，手写仍能上架 | 解析不是唯一入口 |
| 完成的订单里，有一部分留下了记录 | 记录是顺手留下的 |

## 修订

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| 1.0 | 2026-09-23 | 确认评审稿中的默认项，作为最终产品文档 |
